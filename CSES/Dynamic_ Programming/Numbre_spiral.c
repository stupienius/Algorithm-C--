#pragma GCC
#include <stdio.h>
#include <stdlib.h>

long long solve(int n;int a){
    (n%2==0)?
    return
}

int main(){

    int n;

    int i,x,y,ans;
    for(i=0;i<n;i++){
        scanf("%d%d",&x,&y);
        printf("%lld",solve((x>y)?x:y,abs(x-y)));
    }

    return 0;
}
